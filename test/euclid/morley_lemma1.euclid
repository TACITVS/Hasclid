; ============================================================================
; MORLEY'S THEOREM - Lemma 1: Angle Trisector Properties
; ============================================================================
; Test if we can prove basic properties of angle trisectors
; Simplest case: Angles in a right isosceles triangle

:verbose

; Right isosceles triangle at origin
:assume (= xA 0)
:assume (= yA 0)
:assume (= xB 1)
:assume (= yB 0)
:assume (= xC 0)
:assume (= yC 1)

; Angle at A is 90 degrees (π/2)
; Trisected into 3 parts of 30 degrees (π/6) each

; Slope of first trisector from A: tan(π/6) = 1/sqrt(3)
; For simplicity, use the fact that cos(π/6) = sqrt(3)/2, sin(π/6) = 1/2

; LEMMA 1a: Can we prove a simple trisector calculation?
; If angle is 90°, trisection gives 30° angles
; tan(30°) = 1/sqrt(3) ≈ 0.577

; Instead of using transcendental functions, let's use algebraic constraints
; A point P on the first trisector from A satisfies: 3*yP^2 = xP^2 (from tan^2(30°) = 1/3)

:assume (> xP 0)
:assume (> yP 0)
:assume (= (* 3 (^ yP 2)) (^ xP 2))

; This means yP/xP = 1/sqrt(3), or 3*yP^2 = xP^2

; GOAL: Prove that if a point satisfies this constraint, then slope squared = 1/3
:prove (= (* 3 (^ yP 2)) (^ xP 2))
