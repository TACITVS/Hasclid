-- =============================================================================
-- BARROW TANGENT IDENTITY VERIFICATION
-- =============================================================================
-- Given x² = 3, verify that 3x = x³ (which is x + x + x = x*x*x)
-- =============================================================================

:set-timeout 60
:optimize on

:assume (= (^ x 2) 3)
:assume (> x 0)

-- 3x = x³ is equivalent to 3 = x² (when x ≠ 0)
-- Since x² = 3, we have x³ = x * x² = x * 3 = 3x
:auto (= (* 3 x) (^ x 3))

