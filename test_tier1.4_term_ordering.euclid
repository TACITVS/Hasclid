-- TIER 1.4: Better Term Ordering - Test Script

-- Test 1: Check default term ordering
:show-order

-- Test 2: Switch to Lexicographic ordering
:set-order lex
:show-order

-- Test 3: Switch to Graded Lexicographic
:set-order grlex
:show-order

-- Test 4: Switch back to Graded Reverse Lex (default, fastest)
:set-order grevlex
:show-order

-- Test 5: Invalid order (should show error)
:set-order invalid

-- Test 6: Case insensitive
:set-order LEX
:show-order

:set-order GrEvLeX
:show-order

-- Test 7: Define a geometry problem and verify it works with all orderings
:point A 0 0
:point B 3 0
:point C 0 4

-- Prove with default (grevlex)
:show-order
(= (+ (dist2 A B) (dist2 A C)) 25)

-- Prove with lex
:set-order lex
(= (+ (dist2 A B) (dist2 A C)) 25)

-- Prove with grlex
:set-order grlex
(= (+ (dist2 A B) (dist2 A C)) 25)

-- Prove with grevlex again
:set-order grevlex
(= (+ (dist2 A B) (dist2 A C)) 25)

-- Check cache stats (should show multiple entries from different orderings)
:cache-stats

-- End of term ordering tests
