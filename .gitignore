# Compiled executables
Euclid.exe

# Haskell build artifacts
*.hi
*.o

# Editor/OS temporary files
*~
.#*
.DS_Store
*.swp
*.swo

# Cabal build artifacts
dist-newstyle/
.cabal-project/
cabal.project.local
.claude/settings.local.json

# Local cabal home (CABAL_DIR pointed at the repo at some point, so the
# package store, build logs and compiled libraries were committed: ~43 MB of
# .a archives, Windows .exe binaries and per-package logs. Untracked in the
# commit that added this line; the files remain on disk but are no longer
# part of the repository.)
.cabal/

# Local helper scripts
push_refactor.bat

# Local scratch/dev artifacts
scratch/

# Local temporary outputs
tmp_prover_cmds.txt
tmp_prover_out.txt
nul
test_div_detection.hs

# Test results and temporary files
ono_*_results*.txt
TestPrecision*
TestSDP*
*.log
*.exe

