-- Tier 1, Item 1: Better Error Handling - Demonstration
-- This showcases improved error messages across all error types

-- ===========================================
-- SUCCESSFUL OPERATIONS (baseline)
-- ===========================================

-- Define some points
:point A 0 0
:point B 3 4
:point C 5 0

-- Prove Pythagorean theorem (should succeed)
(= (dist2 A B) 25)

-- ===========================================
-- PARSE ERRORS (now with better messages)
-- ===========================================

-- Missing closing parenthesis
(= (+ x 1 9

-- Unexpected closing paren
)

-- Wrong number of arguments to division
:simplify (/ x)

-- Wrong number of arguments to power
(= (^ x y z) 0)

-- Malformed rational number
3//5

-- Unknown operator
(= (foobar x y) 0)

-- Wrong arity for geometric primitive
(= (dist2 A) 0)

-- Formula in expression context
(= (+ (= x 5) 3) 0)

-- Extra tokens after formula
(= x 5) extra stuff

-- ===========================================
-- PROOF ERRORS (clearer failure reasons)
-- ===========================================

-- False equality (now shows normal form clearly)
(= (dist2 A B) 100)

-- ===========================================
-- SUCCESS MESSAGE
-- ===========================================

:list
